# Generation Rules — Project Workflow Alias

This file is the workflow-facing alias for the authoritative generation rules.

## Source of truth

The authoritative generation rules are maintained in:

`00_MASTER/GENERATION_RULES.md`

All specialist accounts must follow `00_MASTER/GENERATION_RULES.md`. This file exists so workflow documents and new ChatGPT workspaces can use the stable `WORKFLOW/GENERATION_RULES.md` path without creating a second conflicting rule set.

## Rule

Do not duplicate or independently redefine generation rules here. If the Master rules change, this file remains an explicit pointer to the Master source.
