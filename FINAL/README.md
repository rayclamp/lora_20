# Final Outputs

Only owner-approved final packages belong here.

Use versioned filenames and do not replace an approved final silently. If a final needs revision, create a new version and record the reason in `06_DIRECTOR/`.
